[package]
name = "prism-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "PRISM core: all extension logic as a pure, dependency-light Rust library (native + wasm32)."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
